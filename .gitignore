node_modules/
.env
*.log
.DS_Store

# Note: responses.jsonl is intentionally NOT ignored — it's the survey data,
# kept in git so it's easy to commit/back up.


# claude --resume 09a5131d-7193-422b-8137-3feac0b8de4c